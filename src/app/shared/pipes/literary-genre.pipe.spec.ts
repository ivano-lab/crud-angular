import { LiteraryGenrePipe } from './literary-genre.pipe';

describe('LiteraryGenrePipe', () => {
  it('create an instance', () => {
    const pipe = new LiteraryGenrePipe();
    expect(pipe).toBeTruthy();
  });
});
